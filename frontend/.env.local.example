# Backend API base URL — no trailing slash.
# Local development:
NEXT_PUBLIC_API_URL=http://localhost:5000
# Production (set this in your hosting platform's env vars, or see .env.production):
# NEXT_PUBLIC_API_URL=https://modernmint-game.onrender.com
