# Production backend — baked into the Next.js client bundle at build time.
# This file IS committed so CI/CD builds pick up the correct URL automatically.
NEXT_PUBLIC_API_URL=https://modernmint-game.onrender.com
